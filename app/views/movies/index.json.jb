@movies.map do |movie|
  {
    id: movie.id,
    title: movie.title,
    year: movie.year,
    director: movie.director,
    english: movie.english,
    plot: movie.plot,
    actors: movie.actors,
    genres: movie.genre_names,
  # url: movie_url(movie, format: :json)
  }
end
